let num = [1,2,3];
let abc = ['a','b','c'];

/*num.push(abc);

console.log(num); appended the abc array at the end as a single element (nested array)



let combine = num.concat(abc);

console.log(combine); combine both the arrays and make a new array with all the values of both the array (no nested array)



let sumation = [...num, ...abc]

console.log(sumation);  same as concat but used widely




console.log(Array.isArray("Nimisha Rawat"));
console.log(Array.from("Nimisha"));  // makes an array of given value
console.log(Array.from({nameID:"nimisha"})); //gives an empty array as it gets confused that it should make the array of key of object given or value of object given

let nestedArray = [1,2,3,4,5,[988,718,381,77,[13,13,13,1],123]];

console.log(nestedArray.flat(Infinity));

let ele1 = 10
let ele2 = 20
let ele3 = 30

console.log(Array.of(ele1,ele2,ele3));


*/



